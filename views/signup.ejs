<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Sign Up | Chat Up</title>
    <script
      src="https://kit.fontawesome.com/97e80ba977.js"
      crossorigin="anonymous"
    ></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="./style.css" />
  </head>
  <body>
    <div class="bg-img">

      <form action="/register" method = "POST" class="container">

        <h1>Sign Up</h1>

        <% if(typeof err != 'undefined'){ %>
          <!-- <h3></h3> -->
          <div class="alert alert-dismissible alert-danger">
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            <strong><%= err %></strong> 
          </div>
        <% } %>

        <label for="name"><b>Name</b></label>
        <input type="text" placeholder="Enter Name" name="name" value="<% typeof name != 'undefined' ? name : '' %>" />

        <label for="email"><b>Email</b></label>
        <input type="email" placeholder="Enter Email" name="email" value="<% typeof email != 'undefined' ? username : '' %>" />

        <label for="password"><b>Password</b></label>
        <input
          type="password"
          placeholder="Enter Password"
          name="password"
          
        />

        <label for="password"><b>Confirm Password</b></label>
        <input
          type="password"
          placeholder="Enter Password"
          name="confirmPassword"
          
        />

        <button type="submit" class="btn">Sign Up</button>
      </form>
      <div class="icon">
        <i class="fa fa-phone-square"></i>
      </div>
      <div class="my-btn">
        <b>Already a Member?</b>
        <a href="/signin" class="s-btn">
          <button class="s-btn">Sign In</button>
        </a>
      </div>
    </div>


    <script src = "https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
